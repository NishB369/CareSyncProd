import ManageQueue from "./ManageQueue";

export const metadata = {
  title: "Manage Queue",
  description:
    "Manage the queue of patients efficiently and ensure smooth operations at the front desk.",
};

export default function StudentPage() {
  return <ManageQueue />;
}
