import LoginPage from "./LoginPage";

export const metadata = {
  title: "Front Desk Operator Login",
  description:
    "Login to access the Front Desk Operator dashboard and manage appointments, patient check-ins, and other front desk operations efficiently.",
};

export default function StudentPage() {
  return <LoginPage />;
}
